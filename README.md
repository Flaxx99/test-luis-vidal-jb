## Tech Stack
- **Frontend:** Angular 21
  -Loading Spinners
  -SweetAlert2
  
- **Backend:** .NET 10
- **Database:** SQL Server

## How to run

### 1. Database
Run the `.sql` script included in this repository to create the database and tables.

### 2. Backend API
1. Open the backend folder.
2. Check `appsettings.json` for the SQL Connection String.
3. Run the API:
   ```bash
   dotnet run
